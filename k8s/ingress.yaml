apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: book-producer-ingress
spec:
  rules:
   - host: hello-world.info
     http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: book-producer-service
                port: 
                  number: 3000
